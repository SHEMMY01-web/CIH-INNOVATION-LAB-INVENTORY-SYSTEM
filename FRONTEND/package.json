{
  "name": "cih-innovation-lab-frontend",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "build": "npm --prefix ../client install && npm --prefix ../client run build && cp -r ../client/dist ./dist"
  }
}
